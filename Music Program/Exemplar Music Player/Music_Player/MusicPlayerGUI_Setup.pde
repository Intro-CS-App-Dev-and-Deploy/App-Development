/*
void musicPlayerGUI_setup() {
 rect(27, 70, 450, 450, 50);
 rect(53, 95, 400, 400, 45); 
 // option for gradient
 //main button area, concentric rings
 
 
 ellipse(250, 300, 350, 350); //outer
 
 //stroke(); // changes middle line
 ellipse(250, 300, 325, 325); //middle
 ellipse(250, 300, 323, 323); 
 //stroke(1);
 ellipse(250, 300, 320, 320); //inside
 
 // option to fill with different colors
 //fill(); //reminder: reset to default to each time
 
 //play, pause button
 if (mouseX>207 && mouseX<296 && mouseY>267 && mouseY<329) {
    fill(hoverOverButton);
    rect(width*1/2-width*1/16, height*1/2-height*1/16, width*2/16, height*2/16, 15); //Courners Rounded more than Outer Rectangle, change?
  } else {
    fill(regularButton);
    rect(width*1/2-width*1/16, height*1/2-height*1/16, width*2/16, height*2/16, 15); //Courners Rounded more than Outer Rectangle, change?
  }; //corners rounded more than outer rect
 /*
 triangle(); // notice X&Y coordinates
 line();
 line();
 
 
 //next and previous buttons
 triangle();// next button
 triangle();
 line();
 
 triangle();// previous button
 triangle();
 line();
 
 //volume buttons
 line(); //volume up
 line();
 
 Line(); // volume down
 
 
 //color for power
 //fill(); /green for play, red for not play. press for end
 ellipse();
 
 //menu button
 ellipse(); // concentric rings
 ellipse();
 ellipse();
 ellipse();
 
 ellipse(); //3-dots
 ellipse();
 ellipse();
 
*/
 
